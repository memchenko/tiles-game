export interface TileInfo {
    image?: string;
    color?: string;
}

export interface TileConfig {
    image?: string;
    color?: string;
    x: number;
    y: number;
    width: number;
    height: number;
}