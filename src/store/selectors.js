import { lensPath } from 'ramda';

export const SCENARIOS_LENS = lensPath(['scenarios', 'descriptions']);
export const SCENARIOS_MAPS = lensPath(['scenarios', 'maps']);
export const SCENARIOS_RESULTS = lensPath(['scenarios', 'results']);
