export enum Directions {
    X,
    Y,
}

export enum Results {
    Normal = 'normal',
    Good = 'good',
    Best = 'best',
}

export enum PerformanceTypes {
    Time = 'time',
    Moves = 'moves',
}
